<UserControl x:Class="SkyQuizApp.Views.Admin.Tabs.QuestionsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1050">

    <UserControl.Resources>
        <Style x:Key="TextBlockCellCentered" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="RowHoverStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E66" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#6C63FF" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellStyleCentered" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="ColumnHeaderCentered" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderBrush" Value="#3F3F52" />
            <Setter Property="BorderThickness" Value="0 0 0 1" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Перегляд питань" FontSize="26" FontWeight="Bold"
                   Foreground="White" Margin="10" Grid.Row="0" />

        <Border Grid.Row="1" Background="#1A1A2C" CornerRadius="8" Padding="10">
            <DataGrid x:Name="QuestionsDataGrid"
                      ItemsSource="{Binding Questions}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserResizeColumns="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      SelectionUnit="FullRow"
                      Background="Transparent"
                      Foreground="White"
                      BorderThickness="0"
                      RowHeight="40"
                      FontSize="14"
                      GridLinesVisibility="None"
                      RowStyle="{StaticResource RowHoverStyle}"
                      CellStyle="{StaticResource CellStyleCentered}"
                      ColumnHeaderStyle="{StaticResource ColumnHeaderCentered}"
                      ScrollViewer.VerticalScrollBarVisibility="Hidden">

                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="Transparent" />
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding QuestionID}" Width="120"
                                        ElementStyle="{StaticResource TextBlockCellCentered}" />
                    <DataGridTextColumn Header="ID тесту" Binding="{Binding TestID}" Width="80"
                                        ElementStyle="{StaticResource TextBlockCellCentered}" />
                    <DataGridTextColumn Header="Текст" Binding="{Binding Text}" Width="3*"
                                        ElementStyle="{StaticResource TextBlockCellCentered}" />
                    <DataGridTextColumn Header="Тип" Binding="{Binding QuestionType}" Width="120"
                                        ElementStyle="{StaticResource TextBlockCellCentered}" />
                    <DataGridTextColumn Header="Створено" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="200"
                                        ElementStyle="{StaticResource TextBlockCellCentered}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>