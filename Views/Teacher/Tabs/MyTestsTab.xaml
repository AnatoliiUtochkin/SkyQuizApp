<UserControl x:Class="SkyQuizApp.Views.Teacher.Tabs.MyTestsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1050"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#6C5DD3" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Створити тест"
                    Command="{Binding CreateTestCommand}"
                    FontSize="14"
                    FontWeight="Bold"
                    Width="160"
                    Height="40"
                    Margin="0,0,10,0"
                    Style="{StaticResource RoundedButtonStyle}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Hidden"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Tests}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"
                                   ItemWidth="300"
                                   HorizontalAlignment="Center" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12"
                                BorderThickness="1"
                                BorderBrush="#3B3F5C"
                                Background="#2C2F48"
                                Margin="10"
                                Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding Title}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="White"
                                           TextWrapping="Wrap" />

                                <TextBlock Grid.Row="1"
                                           Text="{Binding Description}"
                                           FontSize="14"
                                           Foreground="LightGray"
                                           TextWrapping="Wrap"
                                           Margin="0,6,0,0" />

                                <TextBlock Grid.Row="2"
                                           Text="{Binding CreatedAt, StringFormat='Створено {0:yyyy-MM-dd HH:mm}'}"
                                           FontSize="12"
                                           Foreground="Gray"
                                           Margin="0,6,0,0" />

                                <StackPanel Grid.Row="3"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            Margin="0,10,0,0">
                                    <TextBlock Text="{Binding TestKey}"
                                               FontSize="14"
                                               Foreground="#CCCCCC"
                                               Margin="0,0,10,0"
                                               VerticalAlignment="Center" />
                                    <Button Command="{Binding DataContext.CopyKeyCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Width="30"
                                            Height="30"
                                            Background="#444866"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="Скопіювати код">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <Viewbox Width="14" Height="14">
                                                            <fa:IconBlock Icon="Clipboard" Foreground="White" />
                                                        </Viewbox>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>

                                <StackPanel Grid.Row="4"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Right"
                                            Margin="0,15,0,0">
                                    <Button Command="{Binding DataContext.EditTestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Width="36" Height="36"
                                            Background="#6C5DD3"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            Margin="0,0,10,0"
                                            ToolTip="Редагувати">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <Viewbox Width="14" Height="14">
                                                            <fa:IconBlock Icon="Edit" Foreground="White" />
                                                        </Viewbox>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>

                                    <Button Command="{Binding DataContext.DeleteTestCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Width="36" Height="36"
                                            Background="#E74C3C"
                                            BorderThickness="0"
                                            Cursor="Hand"
                                            ToolTip="Видалити">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                                    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                        <Viewbox Width="14" Height="14">
                                                            <fa:IconBlock Icon="TrashCan" Foreground="White" />
                                                        </Viewbox>
                                                    </Grid>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>