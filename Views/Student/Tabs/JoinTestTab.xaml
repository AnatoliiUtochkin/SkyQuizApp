<UserControl x:Class="SkyQuizApp.Views.Student.Tabs.JoinTestTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style TargetType="TextBox" x:Key="RoundedTextBox">
            <Setter Property="Background" Value="#2C2F48" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#6C5DD3" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Height" Value="55" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="#6C5DD3" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="45" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="#2C2F48"
                CornerRadius="12"
                Width="700"
                Height="500">

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
                <TextBlock Text="Код тесту"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,20"
                       TextAlignment="Center" />

                <TextBlock Text="Введіть код доступу, щоб розпочати проходження"
                       FontWeight="SemiBold"
                       Foreground="LightGray"
                       FontSize="16"
                       TextAlignment="Center"
                       Margin="0,0,0,20" />

                <TextBox Style="{StaticResource RoundedTextBox}"
                     Text="{Binding TestKey, UpdateSourceTrigger=PropertyChanged}"
                     Height="55"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     MaxLength="6"
                     CharacterCasing="Upper"
                     Margin="0,0,0,20" />

                <Button Content="Приєднатись"
                    Style="{StaticResource RoundedButton}"
                    Command="{Binding JoinTestCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>