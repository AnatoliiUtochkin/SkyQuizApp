<Window x:Class="SkyQuizApp.Views.Student.ReviewAttemptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Перегляд спроби - SkyQuizApp"
        Height="600" Width="850"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentPinkBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="Montserrat" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Width="22" Height="22" CornerRadius="20" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF6B6B" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" BorderThickness="2" BorderBrush="#6C5DD3">
        <Border.Background>
            <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                <GradientStop Color="#2A2D43" Offset="0" />
                <GradientStop Color="#1E1E2E" Offset="1" />
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal"
                        Grid.Row="0"
                        FlowDirection="RightToLeft"
                        Background="Transparent">
                <Button Content="x"
                        Click="Close_Click"
                        Style="{StaticResource CloseButtonStyle}"
                        Margin="15,10,5,3" />
            </StackPanel>

            <TextBlock Grid.Row="1"
                       Text="Перегляд спроби"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,20" />

            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Disabled"
                          Margin="20,0,20,20">
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding BackgroundColor}"
                                    CornerRadius="10"
                                    Padding="12"
                                    Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="{Binding Text}"
                                               FontWeight="Bold"
                                               FontSize="16"
                                               Foreground="{Binding ForegroundColor}" />

                                    <TextBlock Text="{Binding UserAnswer}"
                                               Foreground="{Binding ForegroundColor}"
                                               FontSize="14"
                                               Margin="0,6,0,0"
                                               FontFamily="Consolas"
                                               TextWrapping="Wrap" />

                                    <TextBlock Text="{Binding CorrectAnswer}"
                                               Foreground="{Binding ForegroundColor}"
                                               FontSize="12"
                                               FontStyle="Italic"
                                               Margin="0,4,0,0"
                                               FontFamily="Consolas"
                                               TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>